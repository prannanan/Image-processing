การแข่งขันนี้เริ่มต้นจากโจทย์ด้าน Image Processing เช่น House Recognition ที่ให้โมเดลแยกภาพว่าเป็น “บ้าน” หรือ “ไม่ใช่บ้าน” โดยใช้ dataset ภาพหลายพันภาพ และวัดผลด้วย Accuracy Score เพื่อดูว่าโมเดลทำนายถูกต้องมากแค่ไหน

ต้องสร้าง AI model ที่จำแนกว่ารูปที่ให้ไปเป็นบ้านหรือไม่ใช่